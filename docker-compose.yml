services:
  r-studio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8787:8787
    volumes:
      - ./src:/home/rstudio/src
    environment:
      - PASSWORD=yourpassword
    container_name: r-studio
    
